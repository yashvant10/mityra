const fs = require('fs');
const path = require('path');

// We need some sample images. Let's find some in the public directory.
const publicDir = path.join(__dirname, '../public');
console.log(publicDir);
